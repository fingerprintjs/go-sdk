name: Bump Internal SDK Requires

on:
  release:
    types:
      - published

jobs:
  bump-internal-sdk-requires:
    name: 'Bump internal go-sdk requirement'
    runs-on: ubuntu-latest
    if: github.event.release.prerelease == false
    steps:
      - uses: actions/checkout@v7
        with:
          ref: main

      - uses: actions/setup-go@v6
        with:
          go-version: '1.26'

      - name: Bump go-sdk requirement in dependent modules
        run: bin/bump-internal-sdk-requires.sh "${{ github.event.release.tag_name }}"

      - name: Generate GitHub App token
        id: app-token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        with:
          app-id: ${{ vars.RUNNER_APP_ID }}
          private-key: ${{ secrets.RUNNER_APP_PRIVATE_KEY }}

      - name: Open pull request
        uses: peter-evans/create-pull-request@5f6978faf089d4d20b00c7766989d076bb2fc7f1 # v8.1.1
        with:
          token: ${{ steps.app-token.outputs.token }}
          commit-message: 'chore: bump internal go-sdk requirement to ${{ github.event.release.tag_name }}'
          title: 'chore: bump internal go-sdk requirement to ${{ github.event.release.tag_name }}'
          branch: chore/bump-internal-sdk-v8
          base: main
          labels: automated
